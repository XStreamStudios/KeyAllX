name: KeyAllX
version: 0.0.10
main: studio.xstream.keyallx.KeyAllX
api-version: '1.17'
author: XStreamStudio
softdepend:
  - PlaceholderAPI
commands:
  keyallx:
    description: Reload the plugin with /keyallx reload
    permission: keyallx.reload

  reset-timer:
    usage: /reset-timer
    description: Reset the keyall timer
    permission: keyallx.reset_timer